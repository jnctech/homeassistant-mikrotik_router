# Test dependencies — matches CI workflow (.github/workflows/ci.yml)
pytest
pytest-homeassistant-custom-component
pytest-cov

# Runtime deps (must match manifest.json)
librouteros>=3.4.1,<4.0
mac-vendor-lookup>=0.1.12
